#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char all;

all = 'a';
while (all <= 'z')
{
	if (char all != q && char all != e)
	{
	putchar(all);
	all++
	}
}
	putchar('\n');
	return (0);
}
